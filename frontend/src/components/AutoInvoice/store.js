// store.js
import { reactive } from 'vue'

export const sopStore = reactive([])
export const invoiceStore = reactive([])
export const gistsStore = reactive([])
export const attachmentsStore = reactive([])
